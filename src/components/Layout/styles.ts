import styled from "styled-components";

export const  Container = styled.div`
  background: var(--primary);
`
export const  Wrapper = styled.div`
  height: 100%; //altura de 100% da tela
  max-width: 1280px; //largura maxima
  margin: 0 auto; // margin 0 pra cima e zero pra baixo
  display: flex;  // psrs poder usar todas as barras
  justify-content: center; //centralizar todo conteudo da nossa leyout
`